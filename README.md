# AirTraffic

A CLI network monitoring tool that shows live network data per application.

## Installation

```bash
pip install -e .
```

## Usage

```bash
sudo airtraffic live
```

This will display live network statistics for each application, updating every 2 seconds.

Press **Ctrl+C** to exit.

## Requirements

- Python 3.7+
- Root/sudo privileges required
